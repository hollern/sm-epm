"""Implemented agents, one module per agent. Each subclasses ``sm_epm.lib.agents.Agent``.

The shared building blocks (the ``Agent`` base class, ``@tool``, toolkits and the ``LLM`` facade)
live in ``sm_epm.lib``. This package holds the agents built from them.
"""
